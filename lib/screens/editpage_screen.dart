import 'package:flutter/material.dart';
class EditRecordScreen extends StatelessWidget {
  const EditRecordScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    AppBar(
      title: const Text('no hay nada pa, andan los wachos.'),
    );
    return Container(
    );
  }
}